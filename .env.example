# Copiá este archivo como .env recién en la HU-03.
# Nunca reemplaces este valor con una API key real dentro del repositorio.
OPENAI_API_KEY=your-key-here
EMBEDDING_MODEL=text-embedding-3-small
RESPONSE_MODEL=
