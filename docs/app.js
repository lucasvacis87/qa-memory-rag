const data=window.QA_MEMORY_SAMPLES.samples;const select=document.querySelector('#case-select');const result=document.querySelector('#result');
for(const sample of data){const option=document.createElement('option');option.value=sample.case_id;option.textContent=`${sample.case_id} · ${sample.user_question}`;select.append(option)}
const escapeHtml=value=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
function render(){const item=data.find(sample=>sample.case_id===select.value)||data[0];const chunks=item.chunks_related.map(chunk=>`<article class="chunk"><h3>${escapeHtml(chunk.id)} · ${escapeHtml(chunk.type)}</h3><p>${escapeHtml(chunk.content)}</p><div class="meta"><span>${escapeHtml(chunk.module)}</span><span>score ${chunk.score}</span></div></article>`).join('');result.innerHTML=`<span class="badge">Evaluación ${item.evaluation.score}/10</span><h3>${escapeHtml(item.user_question)}</h3><p class="answer">${escapeHtml(item.system_answer)}</p><div class="chunks">${chunks||'<p>Sin chunks: abstención controlada.</p>'}</div>`}select.addEventListener('change',render);render();
